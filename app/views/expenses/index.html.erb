<%= link_to new_expense_path do %>
  New
<% end %>

<h1>Index</h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Date</th>
      <th>Category</th>
      <th>Amount</th>
      <th>Description</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @expenses.each do |k,v| %>
      <% v.each do |c| %>
    <tr>
      <td><%= c.date %></td>
      <td><%= c.category.name %></td>
      <td><%= c.amount %></td>
      <td><%= c.description %></td>
      <td><%= link_to 'Edit', edit_expense_path(c.id) %></td>
      <td><%= link_to 'Delete', c, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
      <% end %>
      <tr>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>
