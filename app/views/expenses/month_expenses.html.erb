<h1>Month Expenses</h1>

<div class="card">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Category</th>
        <th>Amount</th>
      </tr>
    </thead>
    <tbody>
      <% @current_month_expenses.each do |key, value| %>
      <tr>
        <td><%= key.name %></td>
        <td><%= value %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
